import { create } from '@storybook/theming/create';

import logo from '../src/assets/arch-logo.png';

export default create({
  base: 'light',
  brandTitle: 'Arch Capital UI Library',
  brandUrl: '#',
  brandImage: logo
});
