import { Meta, Story } from '@storybook/addon-docs/blocks';
import * as stories from './PageAlert.js';

<Meta title="Components / Alert" />

# Alert

The Alert is intended to provide a notification message to a user, placed at the very top of a given page. When active, it pushes the entire website downward to accommodate its height.

<Story name="Default">{stories.Default()}</Story>

<br />

<Story name="With Implementation">{stories.Implementation()}</Story>
